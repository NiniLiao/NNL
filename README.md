# NNL - V33 Login Update

## Updates in V33
1. **New Route**: Login page moved to `/account/login`.
2. **UX Feature**: Added 'Show Password' toggle button in the login form.
3. **Integrity**: Based on the stable V31 fix.

## Install
1. **DELETE** `node_modules`, `.nuxt`, `components` folders.
2. `npm install`
3. `npm run dev`